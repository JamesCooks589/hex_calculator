// Stub for arithmetic functions
export const addHex = (hex1, hex2) => {
    return "0"; // Temporary value (will fail tests)
  };
  
  export const subtractHex = (hex1, hex2) => {
    return "0"; // Temporary value (will fail tests)
  };